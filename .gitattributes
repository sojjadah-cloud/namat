# Normalise to LF in the repository; Git still checks out native endings on
# Windows. Without this the first commit from a Windows machine rewrites every
# line of every file and makes the diff unreviewable.
* text=auto eol=lf

# Binary — never touch these.
*.ttf binary
*.png binary
*.jpg binary
*.ico binary
*.woff  binary
*.woff2 binary
